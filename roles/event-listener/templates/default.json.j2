{
  "ipfs": {
    "gateway_url": "{{ipfs.blockmed_gateway_url}}"
  },
  "ethereum": {
    "contract": {
      "address": "{{ethereum.contract.address}}"
    },
    "websocket_provider": "{{ethereum.websocket_provider}}"
  }
}